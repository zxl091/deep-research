# Windows 自用启停与模型切换

统一项目目录：`E:\desktop\简历项目\industry_information_assistant\industry_information_assistant`。
这是目前实际使用的代码。`deep_reasearch` 下还有其他项目和历史验收产物，不是本项目的启动目录。

首次从仓库下载后，先将 `services.local.example.psd1` 复制为 `services.local.psd1`，再修改其中的环境路径。本地配置不上传 Git。

## 常用命令

在 PowerShell 中先进入目录：

```powershell
cd 'E:\desktop\简历项目\industry_information_assistant\industry_information_assistant'
```

启动全部服务：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\start.ps1
```

停止全部服务：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\stop.ps1
```

重启前后端，并确认依赖服务就绪：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\restart.ps1
```

前端：http://127.0.0.1:5183/ 。后端连通检查：http://127.0.0.1:8000/hello 。

无需自己切换 conda，也无需每次 npm install。脚本使用本机已有 `industry_assistant_clean` 环境。
先打开 Docker Desktop，等待引擎就绪。脚本不会安装依赖、拉取镜像或自动关闭其他项目。
端口冲突时会报错；先关闭占用服务再启动。

## 最简单的模型切换：一条命令

将引号里的值替换成同一 API 平台支持的模型 ID：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\set-model.ps1 -ModelId 'qwen3.7-flash'
```

该命令只修改 `backend/.env` 中的 `OPENAI_MODEL`，然后重启后端；前端不重启，API Key 不变。
如果依赖服务已停止，会先恢复本项目依赖。命令完成表示配置保存且后端启动成功，不代表新模型一定有权限或余额。

也可以手动编辑 `backend/.env`，只改这一行：

```dotenv
OPENAI_MODEL=qwen3.7-flash
```

再运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\restart.ps1 -BackendOnly
```

默认研究、六个研究角色、问答、Text2SQL、记忆摘要均读取这一项，不用再改 Python 中多处模型名称。
Embedding 和 Rerank 是不同用途的模型，不随这一项修改。历史研究记录中的模型名称也不会被改写。
更换 API 提供方时，还要更换 `DASHSCOPE_API_KEY` 和接口地址 `DASHSCOPE_BASE_URL`；若另行设置了 `LLM_BASE_URL`，它优先。
不同模型支持的 JSON、思考模式、输出长度可能不同，切换后应先做一次短请求验证。

研究接口对 Qwen3 系列（包括 qwen3.8-27b）传递 `RESEARCH_ENABLE_THINKING`，默认 `false`，避免更换型号后漏传参数而启用供应商默认思考模式。深度研究的规划、搜索、分析和审核流程仍然执行。
`RESEARCH_MODEL_CALL_TIMEOUT_SECONDS` 默认 `600`，约束单次模型请求从开始到流结束的总时间，超时保留已保存步骤。它不限制整项研究的总时长；需要更长的单次调用时可调整此项并重启后端。原有 HTTP 读写超时不能限制持续返回片段的长请求，因此两者同时保留。

在研究期间停止或重启后端会中断当前步骤。已提交的阶段检查点保留，下次启动后到会话中手动继续；不会自动重新发起研究。尚未保存的当前步骤会重做。

## 目录说明

```text
industry_information_assistant/
  backend/                 后端代码、.env、Python 依赖声明、沙箱源码
  frontend/                前端代码和已有 node_modules
  docker/                  数据库初始化脚本
  data/                    项目本地数据
  docs/                    架构及验收文档
  docker-compose.yml       五个配套服务的定义
  start.ps1                启动入口
  stop.ps1                 停止入口
  restart.ps1              重启入口，可加 -BackendOnly
  set-model.ps1            修改模型并重启后端
  services.local.psd1      本机 conda 路径和现有 Compose 项目名
  scripts/services.ps1     启停实现
  .runtime/                启停状态、进程身份和运行日志（不提交）
```

日志查看：打开 `.runtime/services.json` 中 `stdout`、`stderr` 指向的文件。
旧的 `deep_reasearch/.runtime/industry-assistant` 保留为历史日志，新脚本运行不依赖它。

启动会等待 PostgreSQL、Redis、etcd、MinIO、Milvus 五个服务健康，并检查前端、后端和前端 API 代理。
绘图沙箱容器在需要执行代码时临时创建，平时没有常驻容器；已有镜像 `industry-research-sandbox:local`。
重复启动不会重复创建前后端。停止通过进程启动时间和命令核对身份，只停止本项目记录的进程树及五个指定容器。
停止使用 `docker stop`，不删除容器、数据库和数据卷。

数据库、向量库等持久数据主要存在 Docker 卷中，**只复制代码目录不等于备份数据库**。
Compose 项目名固定为 `industry_information_assistant`，复用现有卷；换目录时不要随意改该名称。
换电脑后先配置环境，并修改 `services.local.psd1` 中的路径；不要直接使用当前机器的绝对路径。
